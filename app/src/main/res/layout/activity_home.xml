<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto"
	android:layout_width="match_parent"
	android:layout_height="match_parent"
	>

	<android.support.v4.view.ViewPager
		android:id="@+id/mHomeViewPager"
		android:layout_above="@+id/mHomeBottomNavLayout"
		android:layout_width="match_parent"
		android:layout_height="match_parent">
	</android.support.v4.view.ViewPager>

	<TextView
		android:id="@+id/mLoadTv"
		android:text="这是一个测试数据"
		android:textSize="28sp"
		android:layout_centerInParent="true"
		android:layout_width="wrap_content"
		android:layout_height="wrap_content"/>

	<android.support.design.widget.BottomNavigationView
		app:labelVisibilityMode="labeled"
		android:id="@+id/mHomeBottomNavLayout"
		android:layout_alignParentBottom="true"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:layout_marginEnd="0dp"
		android:layout_marginStart="0dp"
		app:itemIconTint="@drawable/home_nav_icon_color"
		app:itemTextColor="@drawable/home_nav_text_color"
		android:background="?android:attr/windowBackground"
		app:menu="@menu/home_bottom_nav_menu"/>
</RelativeLayout>